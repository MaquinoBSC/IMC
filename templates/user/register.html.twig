 
{% block body %}
    <style>
        .container{
            background-color: #696969;
        }
        .form{
            background-color: cyan;
            width: 50%;
            margin: auto;
            display: flex;
            justify-content: center;
            flex-direction: row;
        }
    </style>
    <body class="container">
        <div class="form">
            <h1 class="title"> {{ controller_name }} </h1>

            <div>
                {{ form(registerForm) }}
            </div>

            <a href={{ relative_path('http://127.0.0.1:8000/') }}>Login</a>
        </div>
    </body>
{% endblock %}